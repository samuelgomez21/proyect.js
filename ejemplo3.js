let ocupacionCanacha = true 
let nombreCancha = "Bernabeu"

let mensaje = ocupacionCancha ? `la cancha llamada ${nombreCancha} esta ocupada` : `la cancha llamada ${nombreCancha} esta disponible` 
console.log(mensaje)
